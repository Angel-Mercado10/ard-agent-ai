---
description: Initialize an Architecture Record Document for a project
---

Start the canonical `ard` workflow in init mode for: $ARGUMENTS

Use the installed `ard` skill as the source of truth.
If the project already has an ARD, ask whether to overwrite it or switch to `ard-update`.
